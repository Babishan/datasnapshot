- name: Clean up
  hosts: localhost
  tasks:
    - name: stop docker containers
      command: for f in docker ps -a -q; do docker stop $f; done
    - name: remove docker containers
      command: for f in docker ps -a -q; do docker rm $f; done